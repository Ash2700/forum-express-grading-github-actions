{{>tab route='users'}}
<table class="table">
  <thead>
    <tr class="table-dark">
    <th scope ="col">#</th>
    <th scope ="col">Name</th>
    <th scope ="col">Email</th>
    <th scope ="col">Role</th>
    <th scope ="col">#</th>
    </tr>
  </thead>
  <tbody>
    {{#each users}}
    <tr>
    <th scope="col">{{this.id}}</th>
    <td>{{this.name}}</td>
    <td>{{this.email}}</td>
    <td>{{#displayRole this.isAdmin}}admin {{else}} user{{/displayRole}}</td>
    <td>
      <form action="/admin/users/{{this.id}}?_method=PATCH" method="POST">
      <button type="submit" class="btn btn-link">{{#displayRole this.isAdmin}}set as user{{else}} set as admin{{/displayRole}}</button>
      </form>  
      </a>
    </td>
    </tr>
  {{/each}}
  </tbody>
</table>